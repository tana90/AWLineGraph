    import XCTest
    @testable import AWLineGraph

    final class AWLineGraphTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(AWLineGraph().text, "Hello, World!")
        }
    }
